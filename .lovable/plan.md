## 인블로그 현재 상태 (조회 전용)

전체 143개 포스트 기준 실측:

| 항목 | 개수 | 비율 |
|---|---|---|
| image 필드 채워짐 | **141 / 143** | 98.6% |
| custom_scripts.json_ld_script에 FAQPage 포함 | **110 / 143** | 76.9% |

## 해석

- **image 141**: SearchTune 141개 전량 반영 완료. 나머지 2개는 SearchTune 외 포스트.
- **FAQ 110**: `blog_posts.faq_short`를 보유한 글 수(110)와 정확히 일치. 나머지 31건은 원본 DB에 FAQ 데이터 자체가 없어 정상적으로 스킵된 상태.

## 결론

백필은 이미 완료 상태이며 추가 조치는 필요 없습니다. 
FAQ 커버리지를 더 올리려면 원본 `blog_posts.faq_short`에 데이터를 채우는 작업(별도 생성 파이프라인)이 선행되어야 하는데, 이번 요청 범위 밖이므로 계획에 포함하지 않았습니다.

필요하시면 다음 중 하나를 지시해 주세요:
1. FAQ 없는 31건에 대해 AI로 `faq_short` 생성 후 재백필
2. 이대로 종료
